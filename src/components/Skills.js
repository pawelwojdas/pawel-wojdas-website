import React from 'react';
import styles from '../stylesheets/Skills.module.scss';

const Skills = () => {
    return (
        <div className={styles.container} id="skills">Skills</div>
    );
}

export default Skills;
