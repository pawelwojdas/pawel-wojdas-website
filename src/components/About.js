import React from 'react';
import styles from '../stylesheets/About.module.scss';


const About = () => {
    return (
        <div className={styles.container} id="about">About</div>
    );
}

export default About;