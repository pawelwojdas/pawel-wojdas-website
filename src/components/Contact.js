import React from 'react';
import styles from '../stylesheets/Contact.module.scss';


const Contact = () => {
    return (
        <div className={styles.container} id="contact">Contact</div>
    );
}

export default Contact;